/* global describe:true, before:true, after:true, it:true, global:true, process:true */
'use strict';

var expect = require('chai').expect,
	pact = require('./given.js');

describe("Given Spec", function () {

});
